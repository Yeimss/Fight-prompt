/**
 * Composición de dependencias (Composition Root).
 *
 * Aquí se instancian repositorios concretos (infraestructura) y se inyectan
 * en los casos de uso. Es el único lugar que conoce simultáneamente el dominio
 * y sus implementaciones técnicas — el resto del código depende de interfaces.
 *
 * Pendiente de implementación: ver propuesta de arquitectura.
 */
export {};
