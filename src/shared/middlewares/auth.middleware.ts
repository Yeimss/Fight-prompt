/**
 * Middlewares de autenticación y autorización.
 *
 * - authenticate: verifica el access token JWT y adjunta el usuario a la request.
 * - authorize(...roles): RBAC, restringe el acceso por rol (ADMIN/AGENT/USER).
 *
 * Pendiente de implementación.
 */
export {};
