/**
 * Rutas del módulo de usuarios (Fastify plugin).
 *   POST /api/v1/users        (ADMIN)
 *   GET  /api/v1/users        (ADMIN)
 *   GET  /api/v1/users/:id
 * Pendiente de implementación.
 */
export {};
