/**
 * Caso de uso: Login.
 * Verifica credenciales (argon2.verify) y emite access + refresh token.
 * El refresh token rota y se persiste para poder revocarlo.
 * Pendiente de implementación.
 */
export {};
