/**
 * Rutas de autenticación (Fastify plugin).
 *   POST /api/v1/auth/login      (rate-limit estricto)
 *   POST /api/v1/auth/refresh
 *   POST /api/v1/auth/logout
 * Pendiente de implementación.
 */
export {};
